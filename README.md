# An AngularUI Router App

A Simple App which uses the AngularUI Router Module to change views based on the state of the application ( as opposed to ngRoute Module) which changes the view depending on the route of the URL
